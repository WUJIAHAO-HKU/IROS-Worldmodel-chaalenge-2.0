{
  "format": "strict-track2-multichunk-parent-preregistration-v1",
  "created_at": "2026-08-10T23:13:00+08:00",
  "status": "rejected-before-execution",
  "rejected_at": "2026-08-10T23:18:00+08:00",
  "rejection_reason": "Post-preregistration code inspection verified env.train.max_episode_steps=8 and an eight-frame bridge increment, so official GRPO makes exactly one world-model call per episode. The multichunk premise was false and this pilot was never executed.",
  "evaluation_state_at_creation": {
    "seed1240_final128_partial_results_already_observed": true,
    "observed_final128_seeds_or_frames_allowed_for_training": false,
    "observed_final128_metrics_allowed_for_candidate_ranking": false,
    "consequence": "The current final128 remains valid only for the already-frozen seed1240 candidate. Any later parent or policy candidate must use a newly preregistered, disjoint real-RoboTwin acceptance set."
  },
  "structural_motivation": {
    "official_interface": "RETRACTED: later inspection established exactly one 8-frame world-model call per official GRPO episode.",
    "training_mismatch": "The selected reward-aligned parent was optimized and reward-screened on one 8-frame chunk at a time.",
    "hypothesis": "Training on predicted contexts across four contiguous chunks will reduce long-horizon context drift and preserve placement-stage reward progress."
  },
  "immutable_track2_constraints": {
    "policy_base": "official unmodified Pi0.5 adjust_bottle",
    "reward": "official frozen adjust_bottle reward model",
    "rl_algorithm": "official RLinf GRPO",
    "global_steps": 1,
    "participant_mpc_or_candidate_action_selection": false
  },
  "data_boundary": {
    "training": "onpolicy_windows_full128_stride4 train_episodes only",
    "parent_validation": "onpolicy_windows_full128_stride4 validation_episodes only",
    "policy_development22": "policy selection only; never parent training",
    "consumed_final128": "forbidden for all later training and selection",
    "external_or_hidden_evaluation_payloads": "forbidden"
  },
  "pilot": {
    "initialization": "reward_aligned_exact_fp32_probability_400/checkpoint_step_000300",
    "active_arm": "left",
    "chunks": 4,
    "frames": 32,
    "predicted_context_between_chunks": true,
    "detach_at_chunk_boundary": true,
    "steps": 50,
    "checkpoint_interval": 25,
    "batch_size": 1,
    "learning_rate": 3e-7,
    "reward_loss_weight": 0.02,
    "reward_objective": "probability",
    "reward_probability_scale_floor": 1e-5,
    "success_weight": 3.0,
    "late_weight": 3.0,
    "late_start": 64,
    "seed": 1616
  },
  "promotion_gates": {
    "complete_32_frame_parent_validation": true,
    "left_long_horizon_rgb_non_regression": true,
    "left_long_horizon_reward_mae_improvement_percent_min": 3.0,
    "left_long_horizon_terminal_gain_mae_improvement_percent_min": 3.0,
    "existing_single_chunk_visual_and_reward_screen_must_still_pass": true,
    "official_dev22_policy_success_and_grasp_non_regression": true,
    "new_disjoint_real_acceptance_must_be_preregistered_before_evaluation": true
  }
}
