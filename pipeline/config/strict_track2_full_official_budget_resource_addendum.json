{
  "format": "strict-track2-full-official-budget-resource-addendum-v1",
  "created_at": "2026-08-11T08:27:00+08:00",
  "base_contract": "pipeline/config/strict_track2_full_official_budget_contract.json",
  "allowed_resource_only_settings": {
    "actor.enable_offload": true,
    "rollout.enable_offload": true,
    "weight_syncer.patch.transport_device": "cpu",
    "bridge.audit_max_items": 1,
    "service.release_cuda_cache_after_prediction": true,
    "http_observation_transport": "CPU-contiguous"
  },
  "why_semantics_are_unchanged": [
    "offload and reload move identical official tensors between CPU and CUDA only while each worker is idle",
    "the CPU transport retains identical observation values and order",
    "bounded auditing changes only local diagnostic file writing",
    "allocator cache release frees only unreferenced blocks"
  ],
  "still_frozen": {
    "max_epochs": 1000,
    "total_num_envs": 32,
    "group_size": 4,
    "rollout_epoch": 8,
    "max_episode_steps": 200,
    "actor_micro_batch_size": 8,
    "actor_global_batch_size": 6400,
    "save_interval": 10,
    "pi05_weights_and_sampling": true,
    "reward_weights_and_protocol": true,
    "grpo_loss_optimizer_and_hyperparameters": true
  },
  "prohibited": [
    "formal runner.max_steps override",
    "world-model or participant MPC action selection",
    "policy behavior cloning from synthetic data",
    "development22 or final128 data in world-model or policy training",
    "claiming a resource diagnostic or world-model screen as Track2 success"
  ]
}
