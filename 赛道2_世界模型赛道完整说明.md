# WorldArena 2.0 赛道 2：自测到提交 SOP

> **提交物**：动作条件世界模型 HTTPS 服务。  
> **评测目标**：官方用服务生成观测、进行 MBRL 更新，再以更新后策略在留出环境的闭环回报排名。  
> **当前 profile**：`wa2-track2-robotwin-adjustbottle-v1`；历史 5 帧，预测 8 帧，动作 14 维。  
> **最终截止**：2026-08-30（以[挑战赛官网](http://iros2026challenge.world-arena.ai/)为准）。

---

## 1. 执行路线

```text
数据适配 -> 模型单块/滚动自测 -> 官方 API 自测 -> 可选 MBRL 自测 -> 冻结并提交
```

| 阶段 | 必须完成 | 通过条件 | 产出 |
| --- | --- | --- | --- |
| A. 数据 | 读取数据并构造 5+8 样本 | 图像和动作时序正确 | 数据适配器 |
| B. 模型 | 单块和多轮滚动生成 | 模型输出完整且受动作控制 | 冻结 checkpoint |
| C. API | 实现官方 3 个端点 | 合约测试全部通过 | 服务/镜像 |
| D. MBRL | 跑一轮本地策略优化 | reward 非 NaN，策略可完成 rollout | 自测记录 |
| E. 提交 | 冻结服务及版本 | 官方可调用、版本不漂移 | 提交邮件 |

**A、B、C、E 是提交必需；D 强烈建议，但官方没有公开完整最终评测器，无法在本地复现最终排名。**

### 本项目模型选择

首个端到端基线固定为 **iVideoGPT 64 action-conditioned**：代码采用 [THUML/iVideoGPT](https://github.com/thuml/iVideoGPT)（MIT），权重采用 `thuml/ivideogpt-bair-64-act-cond`（MIT）。它已有“视觉历史 + 动作 -> 后续视觉”的动作条件接口和 MBRL 示例，规模约为 114M tokenizer + 138M Transformer，适合先验证全流程。

它与赛道正式 profile 的差异必须明确记录：公开 action-conditioned 权重是在 BAIR 的 64x64、4 维动作和 1 帧条件下训练；本项目把实现适配为 5 帧条件、14 维动作、8 帧预测，并在 Adjust Bottle 数据上微调。**64x64 阶段仅用于跑通训练、滚动、API 和 MBRL 自测，不能作为正式提交模型。**正式提交前须升到官方要求的 `256x256` 并重新完成 C 节全部测试。

为隔离工程问题，pipeline 提供显式 `synthetic` 后端，仅用于 API/时序/MBRL smoke test；它不是 iVideoGPT，不能用于模型效果结论或提交。真实运行使用 `ivideogpt` 后端。

运行入口见 [`pipeline/README.md`](pipeline/README.md)：先运行 synthetic 全流程，再下载 iVideoGPT 权重，执行 `ivideogpt` 后端的训练和同一套测试。

---

## 2. A：数据适配

### 输入

- [WM 微调数据](https://huggingface.co/datasets/TianxingChen/RoboTwin2.0/blob/main/dataset/adjust_bottle/aloha-agilex_clean_50.zip)
- [官方 API](https://github.com/WorldArena2/WorldArena-2.0/blob/main/assets/track2_world_model_service_api_zh.md)
- [官方 profile](https://github.com/WorldArena2/WorldArena-2.0/blob/main/assets/track2_official_profile.json)

### 操作

1. 读取每个 episode 的图像和动作；RoboTwin 示例字段为 `observation/head_camera/rgb`、`joint_action/vector [T,14]`。
2. 图像转为 RGB `uint8`、`256 x 256 x 3`；动作转为 `float32 [14]`。
3. 从一条轨迹构造固定窗口：`5` 张历史帧、`4` 个历史动作、`8` 个未来动作、`8` 张目标帧。
4. 固定动作表示 `robotwin_aloha_agilex_abs14`；从数据/官方 adapter 核对 14 维顺序和单位，不能自行重排或再归一化。

### 时序验收（必须）

```text
历史帧：o0 o1 o2 o3 o4
历史动作：a0 a1 a2 a3
未来动作：u0 ... u7
目标帧：p0 ... p7

a0: o0 -> o1       u0: o4 -> p0
...                 u1: p0 -> p1
a3: o3 -> o4       ...
                    u7: p6 -> p7
```

- [ ] 任意抽样窗口中，历史动作数为 4，未来动作和目标帧数均为 8。
- [ ] 图像形状/类型、动作形状/类型正确；动作与帧按时间从旧到新排序。
- [ ] 记录数据版本、图像预处理、动作转换和 14 维语义。

---

## 3. B：世界模型自测

### B1. 单块测试

输入一条 A 阶段样本：5 张历史帧 + 4 个历史动作 + 8 个未来动作。

- [ ] 输出恰好 8 张 `256 x 256` RGB 帧。
- [ ] 改变未来动作后，输出的机械臂/瓶子运动明显随动作变化。
- [ ] 相同输入与 seed 的输出像素一致。

### B2. 多轮滚动测试

重复多轮：将上一轮预测帧接入下一轮的 5 帧历史，再输入下一组 8 动作。

- [ ] 至少记录一组连续多块 rollout 的输入动作和输出视频/GIF。
- [ ] 检查机械臂、瓶子和场景未快速冻结、跳变或与动作脱钩。
- [ ] 选择通过该测试的 checkpoint，记录权重哈希、代码 commit 和依赖版本。

> 官方会闭环多轮调用；单块帧质量好但滚动不稳定的模型不应提交。

---

## 4. C：官方 API 自测

### C1. 固定服务规格

| 项目 | 必须值 |
| --- | --- |
| 协议 | HTTPS + UTF-8 JSON |
| `api_version` | `"1.0"` |
| `profile_id` | `wa2-track2-robotwin-adjustbottle-v1` |
| 图像 | `png_base64`、RGB、`256 x 256 x 3` |
| 历史/预测 | 5 帧历史、4 历史动作；8 未来动作、8 输出帧 |
| 动作 | `float32 [14]`，`robotwin_aloha_agilex_abs14` |
| state / instruction | `states=null`；instruction 可为字符串、`null` 或省略 |
| 最大 batch / 请求 | 8 个 sample / 33,554,432 bytes |
| 最大并发 / 建议超时 | 1 / 600,000 ms |
| 确定性 | 同输入 + 同 seed -> 相同解码 RGB 像素 |

### C2. 必需端点

| 端点 | 鉴权 | 自测断言 |
| --- | --- | --- |
| `GET /v1/health` | 可不鉴权 | 返回 `ready`、`api_version`、`model_version` |
| `GET /v1/capabilities` | Bearer token | 返回上述 profile 和实际限制；确定性两字段均为 `true` |
| `POST /v1/predict` | Bearer token | 验证请求后返回每个样本 8 张预测帧 |

### C3. `/v1/predict` 自测断言

请求：顶层必须有 `api_version`、UUID `request_id`、冻结的 `model_version`、`profile_id`、`samples`；每个 sample 有唯一 `sample_id`、整数 `seed`、`context.frames[5]`、`context.actions[4][14]`、`context.states=null`、可选 instruction、`actions[8][14]`。

响应：HTTP 200；原样回显 `request_id`、`model_version` 和 `sample_id`；`predictions` 与输入样本同序；每个 `frames` 恰好 8 张。

图像对象为 PNG 原始字节的标准 Base64（无 `data:` 前缀、无换行），且为无 alpha 的 RGB PNG。响应不能包含条件帧、奖励、推荐动作、成功/终止信号、分数或外部 URL。

### C4. 必跑测试矩阵

| 测试 | 预期结果 |
| --- | --- |
| 1 个合法 sample | `200`，1 个 prediction，8 张合规帧 |
| 8 个合法 sample | `200`，8 个 prediction，顺序和 ID 完全一致 |
| 相同输入 + seed | 解码 RGB 像素完全一致 |
| 相同 request ID + 相同 payload | 等价响应 |
| 相同 request ID + 不同 payload | `409 REQUEST_ID_CONFLICT` |
| 坏 PNG、错误帧数/动作维度、NaN/Inf | `400 INVALID_ARGUMENT` |
| 错误 API / 模型版本 / profile | `422 UNSUPPORTED_VERSION` |
| 缺失或错误 token | `401 UNAUTHENTICATED` |
| 请求超过 32 MiB | `413 PAYLOAD_TOO_LARGE` |
| 并发第 2 个请求 | `429 OVERLOADED`，不无限排队 |
| 服务未加载 | `503 NOT_READY` |
| 服务重启后 | `model_version`、capabilities、同 seed 像素结果不变 |

任何一个 sample 无效时，整批返回错误；不能部分成功。错误响应统一为：

```json
{"error":{"code":"...","message":"...","request_id":"...","retryable":false}}
```

---

## 5. D：本地 MBRL 自测（强烈建议）

### 目的

验证世界模型能被策略优化流程使用；这不是官方最终评测的复现。

### 需要补齐的组件

| 组件 | 来源 | 用途 |
| --- | --- | --- |
| 世界模型 | 你们训练的 checkpoint | 生成后续视觉观测 |
| 初始策略权重 | [pi05_adjust_bottle](https://huggingface.co/WorldArena/WorldArena2.0/tree/main/pi05_adjust_bottle) | rollout 起点 |
| 奖励权重 | [reward_model](https://huggingface.co/WorldArena/WorldArena2.0/tree/main/reward_model) | 仅为参数；须用匹配的奖励模型代码加载 |
| 奖励模型代码 + T5 | [RL_env_benchmark](https://github.com/WorldArena2/WorldArena-2.0/tree/main/RL_env_benchmark) 中的 `RoboTwinT5CrossAttn` 配置说明 | 从预测帧计算 reward |
| RL 运行框架/环境 | RLinf + RoboTwin + reset dataset + 依赖 | rollout、奖励、策略更新 |

官方公开的是策略权重、奖励权重、微调数据和集成参考，**不是一键可运行的最终评测包**。在组件和版本未配齐前，本阶段记录为“未完成”，但不阻塞 C 阶段接口验收。

### 操作与通过条件

1. 按 `RL_env_benchmark` 的 Wan/RLinf 示例接入你们的模型核心；本地示例内部可使用 `/reset`、`/chunk_step`，但提交服务仍必须使用第 4 节的 `/v1/...` API。
2. 设置 `condition_frame_length=5`、`chunk=8`、`num_frames=13`、`action_dim=14`。
3. 启动一轮最小 rollout / RL update。

通过：`/reset`、`/chunk_step` 返回成功；reward 非 NaN；至少完成一步策略训练（示例信号为 `Global Step: 1/1`）。保存配置、日志、commit 和结果。

---

## 6. E：冻结与提交

### 冻结清单

- [ ] checkpoint、预后处理、基础模型、依赖、镜像 digest、代码 commit 已冻结。
- [ ] 用上述版本定义唯一 `model_version`；重启后输出不可改变。
- [ ] 服务可公网 HTTPS 访问；`/v1/health` 为 `ready`。
- [ ] token 不出现在仓库、镜像层、日志或公开邮件。

### 邮件提交

收件人：`worldarenav2@outlook.com`  
主题：

```text
{model name}_{version number}_Track2_Submission
```

正文/附件提供：

```text
base_url 或不可变容器 image digest
api_version、model_version、profile_id、capability manifest
启动/健康检查方法、资源需求、技术联系人
模型、数据和第三方依赖的合规声明
Bearer token：按官方指定的独立安全通道发送
```

每队每天最多两次提交；同日超过两次时，官方使用最后两次。评测期间只修复基础设施故障；任何影响输出的改动均按新版本重新提交。

---

## 7. 不可违反的限制

- 只返回预测 RGB 帧；不返回 reward、动作、终止、成功或评分。
- 不保存/训练/分享官方非公开请求数据；不使用人工干预。
- 不针对公开样例硬编码，不探测官方策略、奖励、留出任务或评分。
- 不依据请求顺序、时间、IP、request ID 生成内容。
- 不在响应中加入外部资源、可执行内容、水印、跟踪标记或隐写数据。

## 8. 官方依据

- [赛道 2 说明](https://github.com/WorldArena2/WorldArena-2.0/blob/main/assets/track2_benchmark_description_zh.md)
- [赛道 2 API](https://github.com/WorldArena2/WorldArena-2.0/blob/main/assets/track2_world_model_service_api_zh.md)
- [当前官方 profile](https://github.com/WorldArena2/WorldArena-2.0/blob/main/assets/track2_official_profile.json)
- [总提交指南](https://github.com/WorldArena2/WorldArena-2.0/blob/main/assets/all_submission_guidance.md)
- [本地集成参考](https://github.com/WorldArena2/WorldArena-2.0/tree/main/RL_env_benchmark)
